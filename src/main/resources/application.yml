spring:
  datasource:
    url: jdbc:postgresql://77.83.174.207:5432/urban
    username: postgres
    password: docker

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
      naming-strategy: org.hibernate.cfg.ImprovedNamingStrategy
    properties:
      hibernate:
        dialect:  org.hibernate.dialect.PostgreSQLDialect